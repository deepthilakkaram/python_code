# python_code
def (s:str)-> int:
    last={}
    maxlen=0
    start=0
    for i ,ch and last[ch]>=start:
             if ch in last and last[ch]>=start:
                      start=last[ch]+1
              last[ch]=i
              maxlen=
       
